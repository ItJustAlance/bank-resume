//- svg icon
mixin icon(name,mod)
  if(mod)
    svg(class="svg-icon --color svg-icon--" + name + ' ' + mod)
      use(xlink:href="assets/img/sprite-color.svg#icon-color-" + name)
  else
    svg(class="svg-icon svg-icon--" + name)
      use(xlink:href="assets/img/sprite.svg#icon-" + name)
